import { Auth } from '@/widgets/auth/auth';
import { Image, View } from 'react-native';

export default function HomeScreen() {
  return (
    <View>
      <Auth typePage='registartion' />

    </View>
  );
}

